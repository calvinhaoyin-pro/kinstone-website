# Kinstone Frontend

React + Vite + TypeScript app for the Kinstone marketing site. See the [project root README](../README.md) and [docs/design/](../docs/design/) for full context.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # type-check + production build
npm run lint
```
